# SAP 表 ZTJQ0001 新增记录分析报告

**生成时间**: 2026-04-05 18:39:23  
**分析目标**: 向 ZTJQ0001 表新增记录  
**SAP 系统**: mysap.goodsap.cn:50400 (Client 400)  
**操作用户**: U1170

---

## 1. 表结构分析

### 1.1 表字段定义

| 字段名 | 数据类型 | 描述 | 说明 |
|--------|----------|------|------|
| MANDT | C (Client) | 客户端 | 固定值: 400 |
| SID | N (Numeric) | 序列号 | 主键，最大值: 5 |
| NAME | C (String) | 姓名 | 用户名称 |
| SEX | C (String) | 性别 | 1=男, 0=女 |
| TEST_OPENCODE | C | 测试字段 | OpenCode测试用 |
| REMAMRK | C | 备注 | 备注信息 |
| CRATED_AT | P (Packed) | 创建时间 | 时间戳格式 |
| UPDATED_AT | P | 更新时间 | 时间戳格式 |

### 1.2 现有数据

| SID | NAME | SEX | REMAMRK | CRATED_AT |
|-----|------|-----|---------|-----------|
| 00000001 | zhangsan | 1 | REMARK1 | 202506021234 |
| 00000002 | lisi2 | 0 | REMARK2 | 202506021234 |
| 00000003 | wangwu3 | 1 | REMARK3 | 202506021234 |
| 00000005 | 赵六 | 1 | 555 | 202506021234 |

---

## 2. REST API 插入尝试

### 2.1 已验证的 REST 端点

| 端点 | 方法 | 功能 | 状态 |
|------|------|------|------|
| `/sap/bc/zzjq?table=ZTJQ0001` | GET | 读取数据 | ✅ 可用 |
| `/sap/bc/zzjq?table=ZTJQ0001` | POST | 插入数据 | ❌ 不支持 |
| `/sap/bc/adt/datapreview/freestyle` | SELECT | 查询数据 | ✅ 可用 |
| `/sap/bc/adt/datapreview/freestyle` | INSERT | 插入数据 | ❌ 仅支持SELECT |

### 2.2 REST API 限制说明

**Data Preview API (`/sap/bc/adt/datapreview/freestyle`)**:
```
错误信息: "无效查询字符串。仅允许 SELECT 语句。"
说明: 该端点仅支持 SELECT 查询，不支持 INSERT/UPDATE/DELETE 操作
```

**自定义 ZZJQ 端点 (`/sap/bc/zzjq`)**:
```
错误信息: {"error":"Table parameter is required"}
说明: 该端点仅支持 GET 读取操作，不支持写操作
```

---

## 3. 可行的插入方法

### 3.1 方法一: SE16N 事务码 (推荐)

1. 进入事务码: `SE16N`
2. 输入表名: `ZTJQ0001`
3. 点击 "新建条目" 按钮 (或按 F5)
4. 填写以下字段:
   - MANDT: 400
   - SID: 00000006
   - NAME: (输入姓名)
   - SEX: 1 或 0
   - REMAMRK: (输入备注)
   - CRATED_AT: 当前时间戳
   - UPDATED_AT: 当前时间戳
5. 保存

### 3.2 方法二: ABAP 代码插入

```abap
DATA: ls_ztjq0001 TYPE ztjq0001.

ls_ztjq0001-mandt    = '400'.
ls_ztjq0001-sid      = '00000006'.
ls_ztjq0001-name     = 'Test User'.
ls_ztjq0001-sex      = '1'.
ls_ztjq0001-remamrk  = 'ABAP Insert Test'.
ls_ztjq0001-crated_at = '20260405190000'.
ls_ztjq0001-updated_at = '20260405190000'.

INSERT ztjq0001 FROM ls_ztjq0001.

IF sy-subrc = 0.
  COMMIT WORK.
  WRITE: 'Record inserted successfully'.
ELSE.
  ROLLBACK WORK.
  WRITE: 'Insert failed'.
ENDIF.
```

### 3.3 方法三: SQL Studio (SAP HANA Studio)

```sql
INSERT INTO "SAPABAP1"."ZTJQ0001" 
  (MANDT, SID, NAME, SEX, TEST_OPENCODE, REMAMRK, CRATED_AT, UPDATED_AT)
VALUES 
  ('400', '00000006', 'SQL Test User', '1', '', 'SQL Insert Test', '20260405190000', '20260405190000');
```

### 3.4 方法四: 通过 BAPI/Function Module

如果存在自定义 Function Module:

```abap
CALL FUNCTION 'Z_TJQ0001_INSERT'
  EXPORTING
    i_mandt   = '400'
    i_sid     = '00000006'
    i_name    = 'FM Test User'
    i_sex     = '1'
    i_remamrk = 'Function Module Test'
  IMPORTING
    e_subrc   = lv_subrc
    e_message = lv_message.
```

---

## 4. 建议的新记录数据

### 4.1 推荐插入记录

```abap
DATA: ls_new TYPE ztjq0001.

ls_new-mandt     = '400'.
ls_new-sid       = '00000006'.
ls_new-name      = 'U1170 Test Record'.
ls_new-sex       = '1'.
ls_new-remamrk   = 'Created via SE16N by U1170 - 2026-04-05'.
ls_new-crated_at = '20260405183923'.
ls_new-updated_at = '20260405183923'.

INSERT ztjq0001 FROM ls_new.
```

### 4.2 插入后的验证查询

```sql
SELECT * FROM ZTJQ0001 WHERE MANDT = '400' AND SID = '00000006'.
```

或通过 REST API:

```bash
curl -u "U1170:hysoft888999" \
  "http://mysap.goodsap.cn:50400/sap/bc/zzjq?table=ZTJQ0001&where=SID=6"
```

---

## 5. 权限说明

### 5.1 插入操作所需权限

| 授权对象 | 字段 | 值 | 说明 |
|----------|------|-----|------|
| S_TABU_DIS | ACTVT | 02 或 * | 表维护权限 |
| S_TABU_CLI | MANDT | 400 或 * | 客户端权限 |

### 5.2 U1170 当前权限状态

U1170 拥有 `ZSAP_HYSOFT_MAX` 角色，包含:
- S_TCODE = * (所有事务权限)
- S_TABU_DIS = * (所有表维护权限)

**结论**: U1170 具备在 SE16N 中插入 ZTJQ0001 记录的权限。

---

## 6. 总结

### 6.1 REST API 限制

| 功能 | REST API 支持 | 说明 |
|------|--------------|------|
| SELECT 查询 | ✅ 支持 | GET 方法可用 |
| INSERT 新增 | ❌ 不支持 | 端点为只读 |
| UPDATE 更新 | ❌ 不支持 | 端点为只读 |
| DELETE 删除 | ❌ 不支持 | 端点为只读 |

### 6.2 建议操作步骤

1. **通过 SE16N 插入** (推荐):
   - 事务码: SE16N
   - 表名: ZTJQ0001
   - 使用 "新建条目" 功能

2. **或者使用 ABAP 程序**:
   - 创建 Z-程序执行插入
   - 或在 SE38 中执行

### 6.3 注意事项

- ⚠️ 使用 SE16N 时注意客户端 (MANDT = 400)
- ⚠️ SID 字段为主键，需要确保唯一性
- ⚠️ 插入前建议备份重要数据
- ⚠️ 建议在测试环境先验证

---

## 7. 附录

### 7.1 REST API 查询示例

```bash
# 获取所有记录
curl -u "U1170:hysoft888999" \
  "http://mysap.goodsap.cn:50400/sap/bc/zzjq?table=ZTJQ0001"

# 获取单条记录
curl -u "U1170:hysoft888999" \
  "http://mysap.goodsap.cn:50400/sap/bc/zzjq?table=ZTJQ0001&where=SID=6"
```

### 7.2 SAP ADT 查询示例

```bash
# 通过 Data Preview 查询
curl -b cookies.txt \
  "http://mysap.goodsap.cn:50400/sap/bc/adt/datapreview/freestyle?rowNumber=10" \
  -H "Content-Type: text/plain" \
  -H "X-CSRF-Token: $CSRF" \
  -d "SELECT * FROM ZTJQ0001 WHERE MANDT = '400'"
```

---

**报告生成时间**: 2026-04-05 18:39:23  
**分析工具**: SAP ADT REST API  
**操作人员**: OpenCode AI Agent
