# SAP ADT REST API 端点验证报告

**验证时间**: 2026-04-05 18:47:24  
**端点**: `/sap/bc/adt/navigation/whereused`  
**SAP 系统**: mysap.goodsap.cn:50400 (Client 400)  
**操作用户**: U1170

---

## 1. 端点基本信息

| 属性 | 值 |
|------|-----|
| 端点路径 | `/sap/bc/adt/navigation/whereused` |
| HTTP 方法 | GET |
| 功能描述 | Where-Used 查询（查找对象引用） |
| 预期用途 | 查找 ABAP 对象（如类、函数模块、程序等）的使用位置 |

---

## 2. 验证结果

### 2.1 测试结果汇总

| URL 变体 | HTTP 状态码 | 响应 |
|----------|-------------|------|
| `/sap/bc/adt/navigation/whereused` | 404 | 未找到合适资源 |
| `/sap/bc/adt/navigation/whereused/` | 404 | 未找到合适资源 |
| `/sap/bc/adt/navigation/` | 404 | 未找到合适资源 |
| `/sap/bc/adt/navigation` | 404 | 未找到合适资源 |

### 2.2 详细响应

**请求**:
```bash
curl -u "U1170:hysoft888999" \
  "http://mysap.goodsap.cn:50400/sap/bc/adt/navigation/whereused" \
  -H "Accept: application/vnd.sap.as+xml" \
  -H "X-CSRF-Token: $CSRF"
```

**响应**:
```xml
<?xml version="1.0" encoding="utf-8"?>
<exc:exception xmlns:exc="http://www.sap.com/abapxml/types/communicationframework">
  <namespace id="com.sap.adt"/>
  <type id="ExceptionResourceNotFound"/>
  <message lang="EN">未找到合适资源</message>
  <localizedMessage lang="ZH">未找到合适资源</localizedMessage>
</exc:exception>
```

---

## 3. 相关端点测试

### 3.1 替代端点测试

| URL | HTTP 状态码 | 结果 |
|-----|-------------|------|
| `/sap/bc/adt/nhi` | 404 | 未找到 |
| `/sap/bc/adt/nhi/whereused` | 404 | 未找到 |
| `/sap/bc/adt/links` | 404 | 未找到 |
| `/sap/bc/adt/links/whereused` | 404 | 未找到 |
| `/sap/bc/adt/repository/objects` | 404 | 未找到 |

### 3.2 对比可用端点

| URL | HTTP 状态码 | 结果 |
|-----|-------------|------|
| `/sap/bc/adt/oo/classes/ZTJQ0001` | 406 | 可访问 (Accept 格式问题) |
| `/sap/bc/adt/repository/workspaces` | 404 | 未找到 |
| `/sap/bc/adt/discovery` | 200 | ✅ 可用 |
| `/sap/bc/adt/oo/classes/CL_ADT_WB_RES_APP` | 406 | 可访问 |

---

## 4. 结论

### 4.1 端点状态

| 项目 | 状态 |
|------|------|
| 端点存在 | ❌ **不存在** |
| HTTP 响应 | 404 Not Found |
| 错误类型 | ExceptionResourceNotFound |

### 4.2 可能原因

1. **SICF 服务未激活**: `/sap/bc/adt/navigation` 对应的 SICF 服务可能未激活
2. **SAP 版本限制**: 此端点可能在某些 SAP 版本中不可用
3. **授权问题**: 虽然 U1170 有 S_ADT_RES-URI=*，但特定端点可能被禁用

### 4.3 建议

如需实现 Where-Used 功能，可考虑以下替代方案:

1. **使用 SE80 事务码**: 在 SAP GUI 中使用 SE80 的 Where-Used 功能
2. **使用 ABAP 程序**: 调用 `RS_AUDSEL_AM` 或类似程序
3. **使用 ADT 搜索**: 通过 `/sap/bc/adt/search` 端点搜索对象引用
4. **自定义 REST API**: 开发自定义的 Where-Used REST 服务

---

## 5. 参考信息

### 5.1 SAP 标准 Where-Used 端点（可能需要配置）

```
# 标准 SAP ADT Where-Used 端点（需确认 SICF 激活状态）
/sap/bc/adt/navigation/whereusedin
/sap/bc/adt/nhi/whereused
/sap/bc/adt/links/whereused
```

### 5.2 curl 验证命令

```bash
# Step 1: 获取 CSRF Token
curl -c cookies.txt -u "U1170:hysoft888999" \
  "http://mysap.goodsap.cn:50400/sap/bc/adt/discovery" \
  -H "x-csrf-token: Fetch" -D headers.txt -k > /dev/null

CSRF=$(grep -i "^x-csrf-token" headers.txt | cut -d' ' -f2 | tr -d '\r')

# Step 2: 测试 whereused 端点
curl -b cookies.txt -v \
  "http://mysap.goodsap.cn:50400/sap/bc/adt/navigation/whereused" \
  -H "Accept: application/xml" \
  -H "X-CSRF-Token: $CSRF" -k
```

### 5.3 相关授权对象

| 授权对象 | 字段 | U1170 状态 |
|----------|------|-----------|
| S_ADT_RES | URI | `*` (完全访问) |
| S_DEVELOP | * | `*` (完全权限) |

---

## 6. 附录

### 6.1 HTTP 状态码说明

| 状态码 | 含义 | 说明 |
|--------|------|------|
| 200 | OK | 请求成功 |
| 404 | Not Found | 资源不存在 |
| 406 | Not Acceptable | 不支持的 Accept 格式 |
| 401 | Unauthorized | 未授权 |
| 403 | Forbidden | 禁止访问 |

### 6.2 相关文档

- `20260405200000-SAP-ADT-REST-API完整验证报告.md` - ADT API 验证报告

---

**验证完成时间**: 2026-04-05 18:47:24  
**验证工具**: curl + SAP ADT REST API  
**验证人员**: OpenCode AI Agent
