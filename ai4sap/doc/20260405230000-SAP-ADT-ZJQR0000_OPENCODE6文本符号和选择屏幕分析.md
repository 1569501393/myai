# SAP ADT REST API 获取程序文本符号和选择屏幕

**文档日期**: 2026-04-05  
**SAP 系统**: S/4HANA @ mysap.goodsap.cn:50400  
**用户**: U1170  
**程序**: ZJQR0000_OPENCODE6

---

## 一、程序基本信息

| 属性 | 值 |
|------|-----|
| **程序名称** | ZJQR0000_OPENCODE6 |
| **程序类型** | 可执行程序 (Executable Program) |
| **包** | ZZJQ |
| **创建者** | U1170 |
| **创建时间** | 2026-03-29 |
| **修改时间** | 2026-03-29 |
| **语言** | 中文 (ZH) |
| **Unicode 检查** | 激活 |
| **描述** | OpenCode Material Creation Date Report - MARA Creation Info |
| **消息类** | 00 |

---

## 二、程序源码

```abap
*&---------------------------------------------------------------------*
*& Program ID    : ZJQR0000_OPENCODE2
*& Program Name  : Material Creation Report (CORRECTED)
*& Created By   : OPENCODE
*& Created Date  : 2026-03-22
*& Description   : Query material creation info from MARA
*&                CORRECTED: Uses MARA-ERZET instead of CDHDR
*&---------------------------------------------------------------------*

REPORT zjqr0000_opencode6 MESSAGE-ID 00.

INCLUDE zjqr0000_frm.


*&---------------------------------------------------------------------*
*& Tables
*&---------------------------------------------------------------------*
TABLES: mara.

*&---------------------------------------------------------------------*
*& Types
*&---------------------------------------------------------------------*
TYPES:
  BEGIN OF ty_material,
    matnr TYPE mara-matnr,      " Material Number
    ersda TYPE dats,            " Creation Date
    erzet TYPE uzeit,           " Creation Time (MARA native field)
    ernam TYPE ernam,           " Creator Name
  END OF ty_material.

*&---------------------------------------------------------------------*
*& Data
*&---------------------------------------------------------------------*
DATA: lt_material TYPE TABLE OF ty_material.

DATA: lo_salv TYPE REF TO cl_salv_table,
      lo_cols TYPE REF TO cl_salv_columns_table,
      lo_col  TYPE REF TO cl_salv_column_table,
      lo_func TYPE REF TO cl_salv_functions_list,
      lo_display TYPE REF TO cl_salv_display_settings,
      lo_layout TYPE REF TO cl_salv_layout,
      lv_key TYPE salv_s_layout_key,
      lx_msg TYPE REF TO cx_salv_msg,
      lx_notfound TYPE REF TO cx_salv_not_found.

*&---------------------------------------------------------------------*
*& Selection Screen
*&---------------------------------------------------------------------*
SELECTION-SCREEN BEGIN OF BLOCK b1 WITH FRAME TITLE TEXT-001.

SELECT-OPTIONS:
  s_matnr FOR mara-matnr.    " Material Number Range

SELECTION-SCREEN END OF BLOCK b1.

*&---------------------------------------------------------------------*
*& Start of Selection
*&---------------------------------------------------------------------*
START-OF-SELECTION.
  PERFORM get_data.
  PERFORM display_alv.

*&---------------------------------------------------------------------*
*& Form GET_DATA
*&---------------------------------------------------------------------*
FORM get_data.

  " Direct query from MARA - no need for CDHDR join
*  SELECT matnr, ersda, erzet, ernam
  SELECT *
    INTO CORRESPONDING FIELDS OF TABLE @lt_material
    FROM mara
    WHERE matnr IN @s_matnr
    ORDER BY matnr.

  IF lt_material IS INITIAL.
    MESSAGE 'No data found for the selected criteria' TYPE 'S' DISPLAY LIKE 'W'.
  ENDIF.

ENDFORM.

*&---------------------------------------------------------------------*
*& Form DISPLAY_ALV
*&---------------------------------------------------------------------*
FORM display_alv.

  " Create ALV instance
  TRY.
      cl_salv_table=>factory(
        IMPORTING
          r_salv_table = lo_salv
        CHANGING
          t_table      = lt_material
      ).
    CATCH cx_salv_msg INTO lx_msg.
      MESSAGE lx_msg->get_text( ) TYPE 'E'.
  ENDTRY.

  " Optimize columns
  lo_cols = lo_salv->get_columns( ).
  lo_cols->set_optimize( ).

  " Set column texts
  TRY.
      lo_col ?= lo_cols->get_column( 'MATNR' ).
      lo_col->set_long_text( 'Material Number' ).
      lo_col->set_medium_text( 'Material' ).
      lo_col->set_short_text( 'Mat' ).

      lo_col ?= lo_cols->get_column( 'ERSDA' ).
      lo_col->set_long_text( 'Creation Date' ).
      lo_col->set_medium_text( 'Created Date' ).
      lo_col->set_short_text( 'Date' ).

      lo_col ?= lo_cols->get_column( 'ERZET' ).
      lo_col->set_long_text( 'Creation Time' ).
      lo_col->set_medium_text( 'Created Time' ).
      lo_col->set_short_text( 'Time' ).

      lo_col ?= lo_cols->get_column( 'ERNAM' ).
      lo_col->set_long_text( 'Created By' ).
      lo_col->set_medium_text( 'Creator' ).
      lo_col->set_short_text( 'User' ).

    CATCH cx_salv_not_found INTO lx_notfound.
      MESSAGE lx_notfound->get_text( ) TYPE 'W'.
  ENDTRY.

  " Enable all standard functions
  lo_func = lo_salv->get_functions( ).
  lo_func->set_all( ).

  " Display settings - use abap_true instead of c_on
  lo_display = lo_salv->get_display_settings( ).
  lo_display->set_striped_pattern( abap_true ).
  lo_display->set_list_header( 'Material Creation Report' ).

  " Layout settings
  lo_layout = lo_salv->get_layout( ).
  lv_key-report = sy-repid.
  lo_layout->set_key( lv_key ).
  lo_layout->set_default( abap_true ).

  " Display ALV
  lo_salv->display( ).

ENDFORM.

*&---------------------------------------------------------------------*
*& CORRECTIONS MADE
*&---------------------------------------------------------------------*
* 1. Removed CDHDR join - MARA has creation time field ERZET
* 2. Changed type from cdhdr-utime to uzeit (DDIC base type)
* 3. Changed c_on to abap_true (CL_SALV constant doesn't exist)
* 4. Simplified SQL - no GROUP BY needed
* 5. Used MARA native fields: ERSDA, ERZET, ERNAM
*&---------------------------------------------------------------------*
```

---

## 三、文本符号 (Text Symbols)

### 3.1 文本符号定义

| 文本符号 | 内容 | 用途 |
|----------|------|------|
| **TEXT-001** | (需从文本元素维护) | 选择屏幕框架标题 |

### 3.2 文本符号使用位置

```abap
SELECTION-SCREEN BEGIN OF BLOCK b1 WITH FRAME TITLE TEXT-001.
```

### 3.3 获取文本元素元数据

```bash
# 通过 VIT 端点获取文本元素元数据
curl -u "U1170:hysoft888999" \
  "http://mysap.goodsap.cn:50400/sap/bc/adt/vit/wb/object_type/progpx/object_name/ZJQR0000_OPENCODE6" -k
```

**响应**:
```xml
<?xml version="1.0" encoding="utf-8"?>
<adtcore:mainObject adtcore:name="ZJQR0000_OPENCODE6" adtcore:type="PROG/PX">
  <adtcore:packageRef adtcore:name="ZZJQ"/>
</adtcore:mainObject>
```

---

## 四、选择屏幕 (Selection Screen)

### 4.1 选择屏幕定义

| 属性 | 值 |
|------|-----|
| **屏幕编号** | 1000 |
| **屏幕类型** | 标准选择屏幕 |
| **框架块** | B1 |

### 4.2 选择屏幕参数

```abap
SELECTION-SCREEN BEGIN OF BLOCK b1 WITH FRAME TITLE TEXT-001.

SELECT-OPTIONS:
  s_matnr FOR mara-matnr.    " Material Number Range

SELECTION-SCREEN END OF BLOCK b1.
```

### 4.3 选择屏幕参数说明

| 参数名 | 字段 | 类型 | 说明 |
|--------|------|------|------|
| **S_MATNR** | MARA-MATNR | SELECT-OPTIONS | 物料号范围 |

### 4.4 获取选择屏幕元数据

```bash
# 通过 VIT 端点获取选择屏幕元数据
curl -u "U1170:hysoft888999" \
  "http://mysap.goodsap.cn:50400/sap/bc/adt/vit/wb/object_type/progps/object_name/ZJQR0000_OPENCODE6%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%201000" -k
```

**响应**:
```xml
<?xml version="1.0" encoding="utf-8"?>
<adtcore:mainObject adtcore:name="ZJQR0000_OPENCODE6                      1000" adtcore:type="PROG/PS">
  <adtcore:description>SEL_SCREEN 1000 INFO:170000030000000000000000000001774040365</adtcore:description>
  <adtcore:packageRef adtcore:name="ZZJQ"/>
</adtcore:mainObject>
```

---

## 五、程序结构

### 5.1 数据声明

| 类型 | 名称 | 说明 |
|------|------|------|
| **TABLES** | mara | MARA 表 |
| **TYPE** | ty_material | 物料类型结构 |
| **DATA** | lt_material | 物料内表 |
| **DATA** | lo_salv | ALV 实例 |
| **DATA** | lo_cols | ALV 列对象 |
| **DATA** | lo_col | 单列对象 |
| **DATA** | lo_func | ALV 功能对象 |
| **DATA** | lo_display | ALV 显示设置 |
| **DATA** | lo_layout | ALV 布局对象 |
| **DATA** | lv_key | 布局键值 |
| **DATA** | lx_msg | 消息异常 |
| **DATA** | lx_notfound | 未找到异常 |

### 5.2 类型结构 (TY_MATERIAL)

| 字段 | 类型 | 说明 |
|------|------|------|
| MATNR | MARA-MATNR | 物料号 |
| ERSDA | DATS | 创建日期 |
| ERZET | UZEIT | 创建时间 |
| ERNAM | ERNAM | 创建者 |

### 5.3 事件块

| 事件 | 说明 |
|------|------|
| **START-OF-SELECTION** | 开始选择事件，调用获取数据和显示 ALV |

### 5.4 FORM 例程

| FORM | 说明 |
|------|------|
| **GET_DATA** | 从 MARA 表获取物料创建信息 |
| **DISPLAY_ALV** | 使用 CL_SALV_TABLE 显示 ALV 报表 |
| **FRM_ADD_NUM** | (来自 Include ZJQR0000_FRM) |
| **FRM_WRITE_HELLO** | (来自 Include ZJQR0000_FRM) |

### 5.5 Include 文件

| Include | 说明 |
|---------|------|
| **ZJQR0000_FRM** | 包含 FORM 例程 (FRM_ADD_NUM, FRM_WRITE_HELLO) |

---

## 六、ALV 报表列定义

| 列名 | 长文本 | 中文本 | 短文本 | 字段 |
|------|--------|--------|--------|------|
| **MATNR** | Material Number | Material | Mat | MARA-MATNR |
| **ERSDA** | Creation Date | Created Date | Date | MARA-ERSDA |
| **ERZET** | Creation Time | Created Time | Time | MARA-ERZET |
| **ERNAM** | Created By | Creator | User | MARA-ERNAM |

---

## 七、相关 ADT REST API 端点

### 7.1 获取程序源码

```bash
curl -u "U1170:hysoft888999" \
  "http://mysap.goodsap.cn:50400/sap/bc/adt/programs/programs/ZJQR0000_OPENCODE6/source/main" -k
```

### 7.2 获取程序元数据

```bash
curl -u "U1170:hysoft888999" \
  "http://mysap.goodsap.cn:50400/sap/bc/adt/programs/programs/ZJQR0000_OPENCODE6" -k
```

### 7.3 获取对象结构

```bash
curl -u "U1170:hysoft888999" \
  "http://mysap.goodsap.cn:50400/sap/bc/adt/programs/programs/zjqr0000_opencode6/objectstructure" -k
```

### 7.4 获取文本元素元数据

```bash
curl -u "U1170:hysoft888999" \
  "http://mysap.goodsap.cn:50400/sap/bc/adt/vit/wb/object_type/progpx/object_name/ZJQR0000_OPENCODE6" -k
```

### 7.5 获取选择屏幕元数据

```bash
curl -u "U1170:hysoft888999" \
  "http://mysap.goodsap.cn:50400/sap/bc/adt/vit/wb/object_type/progps/object_name/ZJQR0000_OPENCODE6%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%20%201000" -k
```

### 7.6 获取 Include 源码

```bash
curl -u "U1170:hysoft888999" \
  "http://mysap.goodsap.cn:50400/sap/bc/adt/programs/includes/ZJQR0000_FRM/source/main" -k
```

---

## 八、对象结构分析

```xml
<abapsource:objectStructureElement adtcore:name="ZJQR0000_OPENCODE6" adtcore:type="PROG/P">
  <abapsource:objectStructureElement adtcore:name="MARA" adtcore:type="PROG/PT"/>      <!-- 表 -->
  <abapsource:objectStructureElement adtcore:name="TY_MATERIAL" adtcore:type="PROG/PY"/> <!-- 类型 -->
  <abapsource:objectStructureElement adtcore:name="B1" adtcore:type="PROG/PD"/>        <!-- 选择屏幕块 -->
  <abapsource:objectStructureElement adtcore:name="S_MATNR" adtcore:type="PROG/PD"/>   <!-- 选择选项 -->
  <abapsource:objectStructureElement adtcore:name="START-OF-SELECTION" adtcore:type="PROG/PE"/> <!-- 事件 -->
  <abapsource:objectStructureElement adtcore:name="GET_DATA" adtcore:type="PROG/PU"/>   <!-- FORM -->
  <abapsource:objectStructureElement adtcore:name="DISPLAY_ALV" adtcore:type="PROG/PU"/> <!-- FORM -->
  <abapsource:objectStructureElement adtcore:name="1000" adtcore:type="PROG/PS"/>      <!-- 屏幕 -->
  <abapsource:objectStructureElement adtcore:name="ZJQR0000_FRM" adtcore:type="PROG/I"/> <!-- Include -->
</abapsource:objectStructureElement>
```

### 对象类型说明

| 类型 | 含义 |
|------|------|
| PROG/P | 程序主对象 |
| PROG/PT | 表声明 (TABLES) |
| PROG/PY | 类型定义 (TYPES) |
| PROG/PD | 数据声明/选择屏幕参数 |
| PROG/PE | 事件块 |
| PROG/PU | FORM 例程 |
| PROG/PS | 选择屏幕 |
| PROG/PX | 文本元素对象 |
| PROG/I | Include 程序 |

---

**文档版本**: 1.0  
**最后更新**: 2026-04-05
