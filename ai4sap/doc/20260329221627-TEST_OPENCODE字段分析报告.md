# TEST_OPENCODE 字段分析报告

> 生成时间: 2026-03-30
> 数据来源: SAP S/4HANA 系统 (Client 400)
> 分析表: ZTJQ0003

---

## 1. 字段基本信息

| 属性 | 值 |
|------|-----|
| 字段名 | TEST_OPENCODE |
| 所在表 | ZTJQ0003 |
| 数据类型 | CHAR(1) (推断) |
| 位置 | 第5字段 |
| 值域 | A, B, C |

---

## 2. 数据分布

### 2.1 值分布统计

| TEST_OPENCODE | 记录数 | 占比 |
|---------------|--------|------|
| A | 2 | 50% |
| B | 1 | 25% |
| C | 1 | 25% |

### 2.2 详细记录

| SID | NAME | SEX | TEST_OPENCODE | REMAMRK |
|-----|------|-----|---------------|----------|
| 5 | 555 | 1 (男) | A | 555 |
| 1 | jieqiang | 1 (男) | A | JIEQIANG |
| 2 | 张三 | 1 (男) | B | 张三 |
| 3 | 333 | 1 (男) | C | 333 |

---

## 3. 字段含义推测

### 3.1 命名分析

`TEST_OPENCODE` 字段名称推测含义:
- **TEST**: 表明这是测试用途
- **OPENCODE**: 表明与 OpenCode 集成相关
- **整体**: 用于测试 OpenCode 功能的分类标识

### 3.2 值含义推测

| 值 | 推测含义 | 说明 |
|----|----------|------|
| A | Group A / 测试组A | 2条记录: 555, jieqiang |
| B | Group B / 测试组B | 1条记录: 张三 |
| C | Group C / 测试组C | 1条记录: 333 |

### 3.3 可能用途

1. **功能测试分类**: 区分不同测试场景
2. **数据标记**: 标记数据来源或测试批次
3. **版本标识**: 区分不同版本的测试数据
4. **权限控制**: 不同代码对应不同权限

---

## 4. 与 SEX 字段交叉分析

### 4.1 交叉表

| SEX | TEST_OPENCODE | 记录数 |
|-----|---------------|--------|
| 1 (男) | A | 2 |
| 1 (男) | B | 1 |
| 1 (男) | C | 1 |

**结论**: 所有记录的 SEX 均为 1 (男性)，TEST_OPENCODE 分布与性别无关

---

## 5. 对比分析

### 5.1 ZTJQ0001 vs ZTJQ0003

| 字段 | ZTJQ0001 | ZTJQ0003 | 说明 |
|------|----------|----------|------|
| TEST_OPENCODE | ❌ 不存在 | ✅ 存在 | ZTJQ0003 独有 |
| CRATED_AT | ✅ 存在 | ❌ 不存在 | ZTJQ0001 独有 |
| UPDATED_AT | ✅ 存在 | ❌ 不存在 | ZTJQ0001 独有 |

### 5.2 推测

TEST_OPENCODE 字段是 ZTJQ0003 特有的字段，可能用于:
- 区分测试数据的不同来源
- OpenCode 集成测试的标记
- 数据分类/分组

---

## 6. 域分析

### 6.1 可能的域

由于表未在 SE11 注册，无法确定具体的域定义。可能使用的域:

| 候选域 | 说明 |
|--------|------|
| BU_SEX | SAP 标准性别域 (值: 0,1,2) |
| CHAR1 | 单字符域 |
| XFELD | 布尔域 (X,空) |

### 6.2 建议

如需正式使用，建议在 SE11 中:
1. 创建域 `ZTEST_OPENCODE`
2. 定义固定值: A, B, C
3. 添加描述文本

---

## 7. Python 查询示例

```python
from sap_connector import SAPConnector, load_config
from collections import defaultdict

config = load_config()
conn = SAPConnector(config)

# 获取数据
result = conn.get_table_data('ZTJQ0003', rows=100)
records = result.get('data', [])

# 按 TEST_OPENCODE 分组
groups = defaultdict(list)
for r in records:
    groups[r.get('TEST_OPENCODE')].append(r)

# 统计
for code, rows in groups.items():
    print(f"TEST_OPENCODE = {code}: {len(rows)} 条记录")
    for r in rows:
        print(f"  - {r['NAME']}")
```

---

## 8. 结论

1. **TEST_OPENCODE** 是 ZTJQ0003 表的特有字段
2. **值域**: A (50%), B (25%), C (25%)
3. **用途推测**: 测试数据分类/标记，可能与 OpenCode 集成测试相关
4. **数据特征**: 
   - 所有记录 SEX=1 (男性)
   - NAME 包含数字(555, 333)和中文(张三)以及英文(jieqiang)
5. **与 ZTJQ0001 关系**: ZTJQ0001 无此字段，ZTJQ0003 独有

---

*报告生成: OpenCode AI*
*使用技能: opencode-sap-integration*
