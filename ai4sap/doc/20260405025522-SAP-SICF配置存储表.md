# SAP SICF 配置存储表

**生成时间**: 2026-04-05 02:54:56  
**系统**: SAP S/4HANA @ mysap.goodsap.cn:50400  
**客户端**: 400  
**数据来源**: SAP ADT REST API 查询验证

---

## 一、SICF 服务配置表

### 1.1 核心存储表

| 表名 | 说明 | 状态 |
|------|------|------|
| **ICFSERVICE** | ICF服务定义主表 | ✓ 存在 |
| **ICFHANDLER** | ICF处理器配置表 | ✓ 存在 |
| ~~ICFENTRY~~ | ~~不存在~~ | ✗ 不存在 |

### 1.2 ICFSERVICE 表结构

**用途**: 存储 ICF (Internet Communication Framework) 服务的主配置

| 字段 | 类型 | 说明 |
|------|------|------|
| ICF_NAME | CHAR(255) | ICF服务路径名称 |
| ICFPARGUID | CHAR(32) | 父节点GUID |
| ICFNODGUID | CHAR(32) | 节点GUID |
| ICFALIFLAG | CHAR(1) | 别名标志 |
| ICFCHILDNO | NUMC(4) | 子节点数量 |
| ICFALIASNO | NUMC(4) | 别名编号 |
| ICFNODFLAG | CHAR(1) | 节点标志 |
| ICFALIGUID | CHAR(32) | 别名GUID |
| ICF_EXPAND | CHAR(1) | 展开标志 |
| ICF_MANDT | MANDT | 客户端 |
| ICF_USER | CHAR(12) | ICF用户 |
| ICF_PASSWD | CHAR(120) | 密码(加密) |
| ICF_LANGU | LANG | 语言 |
| OBLUSRFLAG | CHAR(1) | 强制用户标志 |
| PROTSEC | CHAR(1) | 安全协议 |
| X509FLAG | CHAR(1) | X509证书标志 |
| SSLFLAG | CHAR(1) | SSL标志 |
| ALIASLOGIN | CHAR(1) | 别名登录 |
| ICF_AUTH | CHAR(1) | 认证方式 |
| ICF_TCODE | CHAR(20) | 关联事务码 |

### 1.3 ICFHANDLER 表结构

**用途**: 存储 ICF 服务的处理器配置

| 字段 | 类型 | 说明 |
|------|------|------|
| ICF_NAME | CHAR(255) | ICF服务路径名称 |
| ICFPARGUID | CHAR(32) | 父节点GUID |
| ICFORDER | NUMC(4) | 处理器顺序 |
| ICFTYP | CHAR(1) | 处理器类型 |
| ICFHANDLER | CHAR(255) | 处理器类名 |

---

## 二、查询示例

### 2.1 curl 查询 ADT 相关 ICF 服务

```bash
# 获取 CSRF Token
curl -c cookies.txt -u "U1170:密码" \
  "http://mysap.goodsap.cn:50400/sap/bc/adt/discovery" \
  -H "x-csrf-token: Fetch" -D headers.txt -k > /dev/null

CSRF=$(grep -i "^x-csrf-token" headers.txt | cut -d' ' -f2 | tr -d '\r')

# 查询 ADT 相关服务
curl -b cookies.txt \
  "http://mysap.goodsap.cn:50400/sap/bc/adt/datapreview/freestyle?rowNumber=50" \
  -H "Content-Type: text/plain" \
  -H "X-CSRF-Token: $CSRF" \
  -d "SELECT icf_name, icf_user, protsEC from icfservice WHERE icf_name LIKE '%/sap/bc/adt%'"
```

### 2.2 查询 /sap/bc/* 服务

```bash
curl -b cookies.txt \
  "http://mysap.goodsap.cn:50400/sap/bc/adt/datapreview/freestyle?rowNumber=100" \
  -H "Content-Type: text/plain" \
  -H "X-CSRF-Token: $CSRF" \
  -d "SELECT icf_name, icf_user, sslflag, icf_tcode FROM icfservice WHERE icf_name LIKE '/sap/bc/%'"
```

### 2.3 查询处理器配置

```bash
curl -b cookies.txt \
  "http://mysap.goodsap.cn:50400/sap/bc/adt/datapreview/freestyle?rowNumber=50" \
  -H "Content-Type: text/plain" \
  -H "X-CSRF-Token: $CSRF" \
  -d "SELECT icf_name, icforder, icfhandler FROM icfhandler WHERE icf_name LIKE '/sap/bc/adt%'"
```

---

## 三、表关系

```
ICFSERVICE (主表)
├── ICF_NAME (主键)
├── ICF_USER (认证用户)
├── ICF_PASSWD (密码)
├── SSLFLAG (SSL配置)
├── ICF_TCODE (事务码)
└── ...

ICFHANDLER (处理器表)
├── ICF_NAME (外键 → ICFSERVICE.ICF_NAME)
├── ICFPARGUID (父节点GUID)
├── ICFORDER (处理器顺序)
└── ICFHANDLER (处理器类名)
```

---

## 四、常用 ICF 服务路径

| 服务路径 | 说明 |
|----------|------|
| /sap/bc/adt/ | ABAP Development Tools |
| /sap/bc/gui/sap/its/webgui | SAP GUI for HTML (WebGUI) |
| /sap/bc/http/ | HTTP 服务 |
| /sap/bc/rest/ | REST API 服务 |
| /sap/opu/odata/ | SAP Gateway OData 服务 |
| /sap/bc/icf/ | ICF 管理 |

---

## 五、SAP GUI 路径 (SICF)

### 5.1 维护路径

- **事务码**: SMICM (ICM Monitor)
- **事务码**: SICF (激活服务)
- **路径**: System → Configuration → SICF

### 5.2 WebGUI 配置

```
服务路径: /sap/bc/gui/sap/its/webgui
参数: ~transaction=SE24
```

---

## 六、注意事项

1. **ICFSERVICE** 是主表，存储服务的基本配置
2. **ICFHANDLER** 存储服务的处理器配置，一个服务可以有多个处理器
3. 直接修改这些表**不推荐**，应通过事务码 **SICF** 进行管理
4. 服务激活状态也存储在 ICFSERVICE 中

---

**文档生成**: SAP ADT REST API 查询验证  
**生成时间**: 2026-04-05 02:54:56
