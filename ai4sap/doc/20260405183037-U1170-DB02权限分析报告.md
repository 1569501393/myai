# SAP 用户 U1170 DB02 权限分析报告

**生成时间**: 2026-04-05 18:30:37  
**分析用户**: U1170  
**SAP 系统**: mysap.goodsap.cn:50400 (Client 400)

---

## 1. 分析目的

验证用户 U1170 是否具有访问 DB02 事务的权限，并评估是否需要添加额外授权。

---

## 2. DB02 权限要求

DB02（数据库性能监控）是 SAP 数据库管理事务，需要以下授权对象：

| 授权对象 | 说明 | 用途 |
|----------|------|------|
| `S_TCODE` | 事务代码授权 | 允许执行 DB02 事务 |
| `S_DB2_ADM` | DB2 管理权限 | 数据库对象管理 |
| `S_DB2_COMM` | DB2 通信权限 | 数据库通信 |
| `S_DBCON` | 数据库连接 | 数据库连接管理 |
| `S_DBA` (fields) | DBA 字段授权 | DBA_DBHOST, DBA_DBSID, DBA_DBUSER |

---

## 3. 当前授权状态分析

### 3.1 用户角色列表

U1170 当前拥有 **200+ 个角色**，主要包括：

- **仓储管理**: `/SCWM/*` 系列 (24个角色)
- **业务角色**: `SAP_BR_*` 系列 (60+个角色)
- **系统角色**: `SAP_*` 系列 (40+个角色)
- **定制角色**: `ZSAP_HYSOFT_*`, `Z_*` 系列

### 3.2 关键角色分析

| 角色 | S_TCODE | S_DB2_ADM | S_DB2_COMM | S_DBCON | S_DBA Fields |
|------|---------|-----------|------------|---------|--------------|
| **ZSAP_HYSOFT_MAX** | `*` | ACTVT=33 | `*` | `*` | `*` |
| ZSAP_HYSOFT_ALL | `*` | ACTVT=* | `*` | `*` | `*` |
| ZSAP_HYSOFT_ADMIN | - | `*` | `*` | - | `*` |

### 3.3 ZSAP_HYSOFT_MAX 详细授权

```
S_TCODE    TCD  *
S_DB2_ADM  ACTVT 33, DB2OBJ *
S_DB2_COMM ACTVT *, DB2COMNAME *, DB2COMOWN *, DB2COMTYPE *, DB2COMUSER *
S_DBCON    ACTVT *, DBCONNAME *
S_DBA      DBA_DBHOST *, DBA_DBSID *, DBA_DBUSER *
```

---

## 4. 分析结论

### 4.1 DB02 访问权限：✅ 已具备

| 检查项 | 状态 | 说明 |
|--------|------|------|
| DB02 事务访问 | ✅ | S_TCODE = `*` (所有事务) |
| DB2 对象显示 | ✅ | S_DB2_ADM ACTVT=33 |
| DB2 通信权限 | ✅ | S_DB2_COMM = `*` |
| 数据库连接权限 | ✅ | S_DBCON = `*` |
| DBA 字段权限 | ✅ | 所有 DBA_* 字段 = `*` |

### 4.2 权限级别说明

| ACTVT 值 | 含义 | 当前状态 |
|----------|------|----------|
| 33 | 显示/读取 | ZSAP_HYSOFT_MAX |
| * | 完全控制 | ZSAP_HYSOFT_ALL |

**当前状态**: ZSAP_HYSOFT_MAX 提供的 ACTVT=33 允许**显示和监控**数据库状态，但不允许修改操作。

---

## 5. 授权建议

### 5.1 当前配置评估

✅ **无需额外授权** - U1170 已通过 ZSAP_HYSOFT_MAX 角色获得完整的 DB02 访问能力。

### 5.2 可选升级

如需完整的数据库管理权限（可执行修改操作），可添加 **ZSAP_HYSOFT_ALL** 角色：

| 角色 | ACTVT | 权限级别 |
|------|-------|----------|
| ZSAP_HYSOFT_MAX | 33 | 监控/显示 |
| ZSAP_HYSOFT_ALL | * | 完全控制 |

---

## 6. 相关授权对象说明

### 6.1 S_DB2_ADM 字段

| 字段 | 说明 | 授权值 |
|------|------|--------|
| ACTVT | 活动 | 33=显示, *=完全 |
| DB2OBJ | DB2 对象 | `*` = 所有对象 |

### 6.2 S_DB2_COMM 字段

| 字段 | 说明 |
|------|------|
| ACTVT | 活动 |
| DB2COMNAME | DB2 子系统名称 |
| DB2COMOWN | DB2 所有者 |
| DB2COMTYPE | DB2 组件类型 |
| DB2COMUSER | DB2 用户 |

---

## 7. 附录

### 7.1 查询使用的 SAP 表

- `AGR_USERS` - 用户角色分配
- `AGR_1251` - 角色授权数据

### 7.2 SAP ADT REST API 查询

```bash
# 获取用户角色
SELECT UNAME, AGR_NAME FROM AGR_USERS WHERE UNAME = 'U1170'

# 获取角色授权
SELECT OBJECT, FIELD, LOW FROM AGR_1251 
WHERE AGR_NAME = 'ZSAP_HYSOFT_MAX' AND OBJECT LIKE 'S_DB%'
```

---

**结论**: U1170 用户已具备完整的 DB02 访问权限，无需额外添加授权。
