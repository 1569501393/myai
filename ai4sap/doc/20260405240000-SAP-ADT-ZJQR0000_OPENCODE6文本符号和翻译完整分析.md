# SAP ADT REST API 获取程序文本符号和选择屏幕

**文档日期**: 2026-04-05  
**SAP 系统**: S/4HANA @ mysap.goodsap.cn:50400  
**用户**: U1170  
**程序**: ZJQR0000_OPENCODE6

---

## 一、程序基本信息

| 属性 | 值 |
|------|-----|
| **程序名称** | ZJQR0000_OPENCODE6 |
| **程序类型** | 可执行程序 (Executable Program) |
| **包** | ZZJQ |
| **创建者** | U1170 |
| **创建时间** | 2026-03-29 |
| **修改时间** | 2026-03-29 |
| **语言** | 中文 (ZH) |
| **主语言** | 中文 (ZH) |
| **Unicode 检查** | 激活 |
| **描述** | OpenCode Material Creation Date Report - MARA Creation Info |
| **消息类** | 00 |
| **版本号** | 000011 |

---

## 二、程序源码

```abap
*&---------------------------------------------------------------------*
*& Program ID    : ZJQR0000_OPENCODE2
*& Program Name  : Material Creation Report (CORRECTED)
*& Created By   : OPENCODE
*& Created Date  : 2026-03-22
*& Description   : Query material creation info from MARA
*&                CORRECTED: Uses MARA-ERZET instead of CDHDR
*&---------------------------------------------------------------------*

REPORT zjqr0000_opencode6 MESSAGE-ID 00.

INCLUDE zjqr0000_frm.


*&---------------------------------------------------------------------*
*& Tables
*&---------------------------------------------------------------------*
TABLES: mara.

*&---------------------------------------------------------------------*
*& Types
*&---------------------------------------------------------------------*
TYPES:
  BEGIN OF ty_material,
    matnr TYPE mara-matnr,      " Material Number
    ersda TYPE dats,            " Creation Date
    erzet TYPE uzeit,           " Creation Time (MARA native field)
    ernam TYPE ernam,           " Creator Name
  END OF ty_material.

*&---------------------------------------------------------------------*
*& Data
*&---------------------------------------------------------------------*
DATA: lt_material TYPE TABLE OF ty_material.

DATA: lo_salv TYPE REF TO cl_salv_table,
      lo_cols TYPE REF TO cl_salv_columns_table,
      lo_col  TYPE REF TO cl_salv_column_table,
      lo_func TYPE REF TO cl_salv_functions_list,
      lo_display TYPE REF TO cl_salv_display_settings,
      lo_layout TYPE REF TO cl_salv_layout,
      lv_key TYPE salv_s_layout_key,
      lx_msg TYPE REF TO cx_salv_msg,
      lx_notfound TYPE REF TO cx_salv_not_found.

*&---------------------------------------------------------------------*
*& Selection Screen
*&---------------------------------------------------------------------*
SELECTION-SCREEN BEGIN OF BLOCK b1 WITH FRAME TITLE TEXT-001.

SELECT-OPTIONS:
  s_matnr FOR mara-matnr.    " Material Number Range

SELECTION-SCREEN END OF BLOCK b1.

*&---------------------------------------------------------------------*
*& Start of Selection
*&---------------------------------------------------------------------*
START-OF-SELECTION.
  PERFORM get_data.
  PERFORM display_alv.

*&---------------------------------------------------------------------*
*& Form GET_DATA
*&---------------------------------------------------------------------*
FORM get_data.
  SELECT *
    INTO CORRESPONDING FIELDS OF TABLE @lt_material
    FROM mara
    WHERE matnr IN @s_matnr
    ORDER BY matnr.

  IF lt_material IS INITIAL.
    MESSAGE 'No data found for the selected criteria' TYPE 'S' DISPLAY LIKE 'W'.
  ENDIF.
ENDFORM.

*&---------------------------------------------------------------------*
*& Form DISPLAY_ALV
*&---------------------------------------------------------------------*
FORM display_alv.
  TRY.
      cl_salv_table=>factory(
        IMPORTING r_salv_table = lo_salv
        CHANGING  t_table      = lt_material ).
    CATCH cx_salv_msg INTO lx_msg.
      MESSAGE lx_msg->get_text( ) TYPE 'E'.
  ENDTRY.

  lo_cols = lo_salv->get_columns( ).
  lo_cols->set_optimize( ).

  TRY.
      lo_col ?= lo_cols->get_column( 'MATNR' ).
      lo_col->set_long_text( 'Material Number' ).
      lo_col->set_medium_text( 'Material' ).
      lo_col->set_short_text( 'Mat' ).

      lo_col ?= lo_cols->get_column( 'ERSDA' ).
      lo_col->set_long_text( 'Creation Date' ).
      lo_col->set_medium_text( 'Created Date' ).
      lo_col->set_short_text( 'Date' ).

      lo_col ?= lo_cols->get_column( 'ERZET' ).
      lo_col->set_long_text( 'Creation Time' ).
      lo_col->set_medium_text( 'Created Time' ).
      lo_col->set_short_text( 'Time' ).

      lo_col ?= lo_cols->get_column( 'ERNAM' ).
      lo_col->set_long_text( 'Created By' ).
      lo_col->set_medium_text( 'Creator' ).
      lo_col->set_short_text( 'User' ).
    CATCH cx_salv_not_found INTO lx_notfound.
  ENDTRY.

  lo_func = lo_salv->get_functions( ).
  lo_func->set_all( ).

  lo_display = lo_salv->get_display_settings( ).
  lo_display->set_striped_pattern( abap_true ).
  lo_display->set_list_header( 'Material Creation Report' ).

  lo_layout = lo_salv->get_layout( ).
  lv_key-report = sy-repid.
  lo_layout->set_key( lv_key ).
  lo_layout->set_default( abap_true ).

  lo_salv->display( ).
ENDFORM.
```

---

## 三、程序标题翻译 (TRDIRT)

通过 TRDIRT 表查询程序标题的翻译：

```bash
# 获取 CSRF Token
curl -c cookies.txt -u "U1170:hysoft888999" \
  "http://mysap.goodsap.cn:50400/sap/bc/adt/discovery" \
  -H "x-csrf-token: Fetch" -D headers.txt -k > /dev/null

CSRF=$(grep -i "^x-csrf-token" headers.txt | cut -d' ' -f2 | tr -d '\r')

# 查询程序标题
curl -b cookies.txt \
  "http://mysap.goodsap.cn:50400/sap/bc/adt/datapreview/freestyle?rowNumber=5" \
  -H "Content-Type: text/plain" \
  -H "X-CSRF-Token: $CSRF" \
  -d "SELECT NAME, SPRSL, TEXT FROM TRDIRT WHERE NAME = 'ZJQR0000_OPENCODE6'" -k
```

### 查询结果

| 程序名称 | 语言 | 标题 |
|----------|------|------|
| ZJQR0000_OPENCODE6 | 1 (系统) | OpenCode Material Creation Date Report - MARA Creation Info |
| ZJQR0000_OPENCODE6 | E (英文) | 物料报表 |

---

## 四、选择屏幕定义

### 4.1 选择屏幕信息 (D020S)

| 属性 | 值 |
|------|-----|
| **程序** | ZJQR0000_OPENCODE6 |
| **屏幕编号** | 1000 |
| **类型** | S (Selection Screen) |
| **生成时间** | 2026-03-29 16:57:05 |

### 4.2 选择屏幕定义

```abap
SELECTION-SCREEN BEGIN OF BLOCK b1 WITH FRAME TITLE TEXT-001.

SELECT-OPTIONS:
  s_matnr FOR mara-matnr.    " Material Number Range

SELECTION-SCREEN END OF BLOCK b1.
```

### 4.3 选择屏幕参数

| 参数名 | 类型 | 字段 | 说明 |
|--------|------|------|------|
| **S_MATNR** | SELECT-OPTIONS | MARA-MATNR | 物料号范围 |

---

## 五、字段标签翻译 (DD04T)

通过 DD04T 表查询字段标签的多语言翻译：

```bash
curl -b cookies.txt \
  "http://mysap.goodsap.cn:50400/sap/bc/adt/datapreview/freestyle?rowNumber=20" \
  -H "Content-Type: text/plain" \
  -H "X-CSRF-Token: $CSRF" \
  -d "SELECT ROLLNAME, DDLANGUAGE, SCRTEXT_S, SCRTEXT_M, SCRTEXT_L FROM DD04T WHERE ROLLNAME = 'MATNR'" -k
```

### MATNR 字段标签翻译

| 语言 | 短文本 (SCRTEXT_S) | 中文本 (SCRTEXT_M) | 长文本 (SCRTEXT_L) |
|------|---------------------|---------------------|---------------------|
| 1 (系统) | 物料 | 物料 | 物料 |
| E (英文) | Material | Material | Material |
| D (德文) | Material | Material | Material |
| F (法文) | Article | Article | Article |
| J (日文) | 品目 | 品目 | 品目 |
| K (韩文) | 자재 | 자재 | 자재 |
| 3 (泰文) | วัสดุ | วัสดุ | วัสดุ |
| R (俄文) | Матеріал | Матеріал | Матеріал |
| A (阿拉伯文) | مادة | مادة | مادة |
| H (希伯来文) | חומר | חומר | חומר |

---

## 六、数据元素信息 (DD04L/DD04T)

通过 ADT API 获取 MATNR 数据元素信息：

```bash
curl -u "U1170:hysoft888999" \
  "http://mysap.goodsap.cn:50400/sap/bc/adt/ddic/dataelements/MATNR" -k
```

### 数据元素详情

| 属性 | 值 |
|------|-----|
| **名称** | MATNR |
| **类型** | 数据元素 (DTEL/DE) |
| **域** | MATNR |
| **数据类型** | CHAR |
| **长度** | 40 |
| **包** | BMG |
| **描述** | Materialnummer |
| **主语言** | 德文 (DE) |

---

## 七、程序对象结构

通过 ADT API 获取程序对象结构：

```bash
curl -u "U1170:hysoft888999" \
  "http://mysap.goodsap.cn:50400/sap/bc/adt/programs/programs/zjqr0000_opencode6/objectstructure" -k
```

### 对象结构列表

| 对象名称 | 类型 | 说明 |
|----------|------|------|
| ZJQR0000_OPENCODE6 | PROG/P | 程序主对象 |
| MARA | PROG/PT | 表声明 |
| TY_MATERIAL | PROG/PY | 类型定义 |
| B1 | PROG/PD | 选择屏幕块 |
| S_MATNR | PROG/PD | 选择选项 |
| LT_MATERIAL | PROG/PD | 内表变量 |
| LO_SALV | PROG/PD | ALV 实例 |
| LO_COLS | PROG/PD | ALV 列对象 |
| LO_COL | PROG/PD | 单列对象 |
| LO_FUNC | PROG/PD | ALV 功能对象 |
| LO_DISPLAY | PROG/PD | ALV 显示设置 |
| LO_LAYOUT | PROG/PD | ALV 布局对象 |
| LV_KEY | PROG/PD | 布局键值 |
| LX_MSG | PROG/PD | 消息异常 |
| LX_NOTFOUND | PROG/PD | 未找到异常 |
| START-OF-SELECTION | PROG/PE | 事件块 |
| GET_DATA | PROG/PU | FORM 例程 |
| DISPLAY_ALV | PROG/PU | FORM 例程 |
| FRM_ADD_NUM | PROG/PU | FORM 例程 (Include) |
| FRM_WRITE_HELLO | PROG/PU | FORM 例程 (Include) |
| 1000 | PROG/PS | 选择屏幕 |
| ZJQR0000_FRM | PROG/I | Include 文件 |

---

## 八、文本符号 (TEXT Symbols)

### 8.1 文本符号引用

程序中引用了以下文本符号：

| 文本符号 | 使用位置 | 说明 |
|----------|----------|------|
| **TEXT-001** | 选择屏幕框架标题 | 块 B1 的框架标题 |

### 8.2 获取文本符号的方法

文本符号存储在程序文本池中，可通过以下方法获取：

#### 方法1: 通过 VIT 端点

```bash
curl -u "U1170:hysoft888999" \
  "http://mysap.goodsap.cn:50400/sap/bc/adt/vit/wb/object_type/progpx/object_name/ZJQR0000_OPENCODE6" \
  -H "Accept: application/vnd.sap.adt.basic.object.properties+xml" -k
```

#### 方法2: 通过 SE16N 查询 (需 SAP GUI)

```
事务码: SE16N
表名: D020T
条件: PROG = 'ZJQR0000_OPENCODE6'
```

#### 方法3: 通过 ABAP 代码

```abap
DATA: lt_textpool TYPE TABLE OF textpool.

READ TEXTPOOL 'ZJQR0000_OPENCODE6' INTO lt_textpool
  LANGUAGE sy-langu.

LOOP AT lt_textpool INTO DATA(ls_textpool).
  WRITE: / ls_textpool-key, ls_textpool-entry.
ENDLOOP.
```

### 8.3 文本符号表结构 (D020T)

| 字段 | 类型 | 说明 |
|------|------|------|
| PROG | CHAR | 程序名称 |
| DYNR | NUMC | 屏幕编号 |
| LANG | CHAR | 语言代码 |
| DTXT | CHAR | 屏幕文本 |

---

## 九、选择屏幕参数表结构 (D020S)

| 字段 | 类型 | 说明 |
|------|------|------|
| PROG | CHAR | 程序名称 |
| DNUM | CHAR | 选择屏幕编号 |
| TYPE | CHAR | 屏幕类型 (S=Selection) |
| FNUM | CHAR | 屏幕编号 |
| SPRA | CHAR | 语言代码 |
| CUPO | CHAR | 屏幕信息 |
| DGEN | DATS | 生成日期 |
| TGEN | TIMS | 生成时间 |

---

## 十、ALV 报表列定义

通过代码中动态设置的 ALV 列文本：

| 列名 | 长文本 | 中文本 | 短文本 | 数据源字段 |
|------|--------|--------|--------|------------|
| **MATNR** | Material Number | Material | Mat | MARA-MATNR |
| **ERSDA** | Creation Date | Created Date | Date | MARA-ERSDA |
| **ERZET** | Creation Time | Created Time | Time | MARA-ERZET |
| **ERNAM** | Created By | Creator | User | MARA-ERNAM |

---

## 十一、相关 ADT REST API 端点

| 端点 | 方法 | 说明 |
|------|------|------|
| `/sap/bc/adt/programs/programs/{name}/source/main` | GET | 获取程序源码 |
| `/sap/bc/adt/programs/programs/{name}` | GET | 获取程序元数据 |
| `/sap/bc/adt/programs/programs/{name}/objectstructure` | GET | 获取程序对象结构 |
| `/sap/bc/adt/vit/wb/object_type/progpx/object_name/{name}` | GET | 获取文本元素元数据 |
| `/sap/bc/adt/vit/wb/object_type/progps/object_name/{name}` | GET | 获取选择屏幕元数据 |
| `/sap/bc/adt/programs/includes/{name}/source/main` | GET | 获取 Include 源码 |
| `/sap/bc/adt/ddic/dataelements/{name}` | GET | 获取数据元素定义 |
| `/sap/bc/adt/datapreview/freestyle` | POST | SQL 查询 |

---

## 十二、SQL 查询示例

### 12.1 查询程序标题翻译

```sql
SELECT NAME, SPRSL, TEXT 
FROM TRDIRT 
WHERE NAME = 'ZJQR0000_OPENCODE6'
```

### 12.2 查询字段标签翻译

```sql
SELECT ROLLNAME, DDLANGUAGE, SCRTEXT_S, SCRTEXT_M, SCRTEXT_L 
FROM DD04T 
WHERE ROLLNAME = 'MATNR'
```

### 12.3 查询选择屏幕定义

```sql
SELECT PROG, DNUM, TYPE, FNUM, SPRA, CUPO, DGEN, TGEN 
FROM D020S 
WHERE PROG = 'ZJQR0000_OPENCODE6'
```

### 12.4 查询程序基本信息

```sql
SELECT NAME, VARCL, SUBC, CNAM, CDAT, UNAM, UDAT, VERN, FIXPT, LDBNAME, UCCHECK 
FROM TRDIR 
WHERE NAME = 'ZJQR0000_OPENCODE6'
```

---

## 十三、限制说明

### 13.1 文本符号获取限制

- **ADT REST API 不直接提供文本符号内容**: 文本符号存储在程序的文本池中，ADT API 只能获取元数据
- **需要 SAP GUI 或 ABAP 代码**: 要获取完整的文本符号内容，需要使用 SE16N 或编写 ABAP 代码
- **建议使用 SE16N**: 通过 D020T 表查询程序的选择屏幕文本翻译

### 13.2 推荐工具

| 工具 | 用途 |
|------|------|
| SE16N | 查询表数据 |
| SE37 | 函数模块 |
| SE80 | 对象浏览器 |
| SE93 | 事务码维护 |

---

**文档版本**: 1.0  
**最后更新**: 2026-04-05
